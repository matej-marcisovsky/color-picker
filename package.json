{
  "name": "color-picker",
  "version": "1.0.0",
  "description": "",
  "main": "background.js",
  "devDependencies": {
    "@types/chrome": "0.0.154",
    "rollup": "^2.56.3",
    "typescript": "^4.4.2"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc && rollup build/Content.js --file dist/Content.js --format iife && rollup build/Background.js --file dist/Background.js --format iife"
  },
  "author": "Matěj Marčišovský",
  "license": "MIT"
}
